import React from "react";

function AdminPage() {
  return (
    <div>
      Perfect for Teams Git branches for multiple fake API, pull requests to
      discuss and update your API. Secure, Mockend can only access the config
      file and not the rest of your code. Nothing to install, use your favorite
      tools: GitHub, VSCode, CodeSandbox, Terminal, …
    </div>
  );
}

export default AdminPage;
